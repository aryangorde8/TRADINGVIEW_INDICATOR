# TRADING_SYSTEM_RECON — code-grounded audit for resume + interview defense

*Generated by reading the actual source. Every claim cites `file:line`. Where
docs and code disagree, the code wins and the discrepancy is listed. The
decisive distinction throughout: **reproducible-from-this-repo** (a committed
script/data regenerates it) vs. **asserted-only** (a number that lives in a
comment/doc with no committed artifact proving it).*

---

## 1. ONE-PARAGRAPH SUMMARY

This repo is a personal quantitative-trading research system for NSE Indian
equities, plus a genuinely engineered support tool. The trading core is a
**Stan Weinstein Stage-2 weekly trend strategy** implemented in Pine Script
(`src/stock_stage2_trend_weekly.pine`) — long-only cash equities, enter on a
weekly close making a new 52-week high above a rising 30-week SMA, exit on a
weekly close below that SMA. Around it sit Python tools: a **weekly Stage-2
scanner** over any watchlist (`tools/stage2_scan.py`), a set of **historical
replication scripts** that recompute the strategy's profit factor with
bootstrap confidence intervals and transaction costs (`tools/research/`), and
an **MIT-licensed `fallacy-auditor` package** (`fallacy-auditor/`) that audits
trading *reasoning* for five named fallacies using a local Ollama LLM, whose
signature feature is a **grounding gate** that discards any model finding not
quoted verbatim from the input. The engineering (the auditor: grounding gate,
schema validation, verifier pass, Pine linter, profit-factor fragility
auditor, 79 tests, CI) is the most defensible part. The trading *performance
numbers* are produced by real, runnable code but are **not committed as
artifacts, depend on live Yahoo data that drifts, and use survivorship-biased
universes** — so they are demonstrations of methodology, not verifiable track
records.

---

## 2. CAPABILITY REALITY CHECK

| Capability | Verdict | Evidence |
|---|---|---|
| Weinstein Stage-2 Pine strategy | **REAL** | `src/stock_stage2_trend_weekly.pine:66-85` — entry/exit logic as coded; long-only |
| Python weekly scanner | **REAL** | `tools/stage2_scan.py:61-100` classify(); completed-weeks filter `:84`; breadth log `:141-157` |
| fallacy-auditor: grounding gate | **REAL, correctly wired** | `grounding.py:36` substring check; `audit.py:56,63` returns only grounded |
| fallacy-auditor: Ollama LLM integration | **REAL (needs Ollama running + model pulled)** | `llm.py:134-165` `/api/chat`; default `qwen2.5:7b` `:46` |
| fallacy-auditor: two-pass verifier | **REAL** | `audit.py:83-87`; `verifier.py` (1:1 reconciliation) |
| Eval harness (precision/recall) | **REAL, but numbers not committed** | `tests/test_eval.py:132-193`; writes `eval_report.json` which is **gitignored** (`.gitignore:5`) |
| Hand-labeled eval set | **REAL, committed** | `data/labeled_examples.jsonl` — 44 examples, 38 labels, 10 clean (verified by count) |
| 79-test suite | **REAL** | `pytest --collect-only` → 79 tests (1 eval test deselected) |
| Pine linter | **REAL** | `pinelint.py:1-29` rules; regex-based, no deps |
| Profit-factor fragility auditor | **REAL** | `profit.py:137-151` seeded 1000-round bootstrap CI; `:153` audit_trades |
| Trade journal | **REAL (empty until you log trades)** | `tools/journal.py:36-40` cost-applied P&L; `journal.csv` gitignored |
| Greenblatt value screen | **REAL (depends on Yahoo fundamentals quality)** | `tools/value_screen.py:45-69` |
| Historical replications (PF/CAGR numbers) | **REAL code, results NOT committed, data drifts** | `tools/research/replicate_stage2.py:104-124` cost-applied P&L; universes hardcoded `:20,23` |
| Paid Claude engines (Fable/Opus) | **SCAFFOLDED** (optional, needs `[anthropic]` extra + credentials) | `llm.py:191-199,208-288` lazy import |

Nothing is SIMULATED/stubbed in a deceptive way. The honest split is
**REAL-but-requires-a-runtime-dependency** (Ollama for the auditor; internet+
yfinance for the scanner/replications) and **REAL-but-results-not-committed**
(all performance numbers).

---

## 3. BACKTEST INTEGRITY REPORT (most important)

### 3a. Lookahead / repainting — **VERDICT: CLEAN**
- **Pine strategy:** the 52-week anchor is `ta.highest(close[1], inAnchorLen)`
  (`stock_stage2_trend_weekly.pine:67`) — it uses the *prior* bar's close, so
  today's close can legitimately break it without self-reference. Orders use
  `process_orders_on_close = true` and `calc_on_every_tick = false`
  (`:43-44`), i.e. signals evaluate on the confirmed bar close. There is **no
  `request.security`** anywhere (the only textual match is a comment at
  `:34`; grep confirms zero calls) — so no multi-timeframe lookahead vector.
- **Python scanner:** `tools/stage2_scan.py:84` — `w = w[w.index <=
  pd.Timestamp.now()]` drops the still-forming week (W-FRI labels each week by
  its future Friday), so a mid-week run judges the last *completed* weekly
  bar. The anchor is `w.shift(1).rolling(52).max()` (`:88`) — prior weeks only.
- **The one honest caveat (LIVE, not backtest):** because signals are decided
  on the weekly close, live trading must wait for Friday's close; acting
  mid-week is a forming-bar and could un-fire. The code enforces this for the
  scanner; a human could still jump the gun. This is a discipline point, not a
  code defect.

### 3b. Survivorship — **VERDICT: CONCERN (acknowledged, but real)**
- The replication universes are **hardcoded lists of currently-listed names**
  (`tools/research/replicate_stage2.py:20-25`; `tools/watchlist_nifty500.txt`
  is today's index members). Delisted/failed companies are absent. Trend-
  following results are the *most* inflated by this bias (dead names trend to
  zero and would add losers).
- The Pine header states this plainly (`:26-27`), and the wide-universe script
  header repeats the caveat (`tools/research/replicate_stage2_universe.py`
  docstring). So it is disclosed — but it means **the pooled PF figures are an
  upper bound, not an unbiased estimate.** The more defensible readout is
  *breadth* (% of names profitable), which is less sensitive to it, and the
  explicit floor (~15-16% CAGR) after stripping the tail.
- Free data (yfinance) has no point-in-time delisted-universe feed, so this is
  a genuine, unfixable-with-free-tools limitation — state it, don't hide it.

### 3c. Cost modeling — **VERDICT: CLEAN but INCONSISTENT between the two engines**
- **Python replications apply 0.25%/side** in the actual P&L math:
  `replicate_stage2.py:105-108` — `proceeds = row.c*qty*(1-COST)`, `cost_basis
  = entry_px*qty*(1+COST)`, `COST = 0.0025` (`:28`). The reported pooled PFs
  come from this path. Costs are genuinely in the numbers.
- **The Pine strategy applies 0.2%/side + 5 ticks slippage**
  (`stock_stage2_trend_weekly.pine:49-50`: `commission_value = 0.2`,
  `slippage = 5`). So TradingView's own tester and the Python replication use
  **different cost assumptions** (0.2% vs 0.25%). Not wrong, but a skeptic will
  notice the two "backtests" aren't the same experiment. State which number
  came from which engine.
- Minor: `replicate_stage2.py` contains a superseded `simulate()` function
  (`:~40-66`, note the odd `equity += ... + 0  # handled below` at `:60`) that
  is **not** the path producing the reported numbers — the `run()` function's
  inline loop (`:100-124`) is. Dead code; harmless to results but worth
  deleting.

### 3d. Parameter provenance — **VERDICT: LOW CONCERN**
- 52 / 30 / 4 are hardcoded inputs (`stock_stage2_trend_weekly.pine:58-60`)
  and in the research scripts. 30-week SMA and 52-week high are **Weinstein's
  canonical, published values** (1988) — chosen a priori, not fit. The 4-week
  "rising" lookback is a design choice, not obviously optimized.
- **Cannot verify from code** that no in-sample tuning happened (the process
  isn't in the repo) — but there is no grid-search / optimizer artifact in the
  code, and the comments repeatedly forbid tuning (`:35-36`, `:55`). Defensible
  as "fixed literature parameters," not as "we proved they're optimal."

### Reproducible-from-repo vs. asserted-only (decisive for the resume)
| Reported number | Status |
|---|---|
| PF 3.53 / 3.78 (39-name universes) | **Committed** — `results/backtests/*_pooled.csv` + `*_audit.txt`; regenerable (drifts with Yahoo); survivorship-biased |
| PF 6.29 (Nifty 500), 2.44 (US) | Same — real script (`replicate_stage2_universe.py`), **no committed output** |
| 23.8% CAGR / 23% maxDD (portfolio) | **Committed** — `results/backtests/stage2_portfolio_snapshot.txt` (2026-07-09 run: 23.9%/23.6% modern; 25.3% full) |
| Eval two-pass (committed: P 0.87 / R 0.71) | **Committed** — `fallacy-auditor/results/eval_report_snapshot.json`; regenerable with Ollama (`pytest -m eval -s`), runs vary a few points |
| 44 examples, 79 tests, 5 fallacies | **Fully committed and verifiable** in the repo right now |

**Bottom line:** the *methodology and code* are reproducible and real; the
*performance figures* are not committed artifacts, drift with live data, and
carry survivorship bias. Put the engineering on the resume, and phrase any
number as "in backtests I ran," never as a track record.

---

## 4. STACK INVENTORY

| Layer | What's actually used | Source |
|---|---|---|
| Strategy DSL | Pine Script v6 (TradingView) | `src/stock_stage2_trend_weekly.pine:1,38-52` |
| Language | Python ≥3.11 | `fallacy-auditor/pyproject.toml:10` |
| Core deps (auditor) | `pydantic>=2.5` only; stdlib `urllib` for HTTP | `pyproject.toml:15-17`; `llm.py:30-31` |
| Optional deps | `anthropic>=0.40` (paid engines), `pytest>=8` (dev) | `pyproject.toml:19-21` |
| Tooling deps | `pandas`, `yfinance` (scanner/research) | `tools/stage2_scan.py:34-35` |
| Local LLM | Ollama, default model **`qwen2.5:7b`**, `/api/chat`, temp 0, schema-constrained | `llm.py:46,134-159` |
| Data source | Yahoo Finance via `yfinance` (`auto_adjust=True`) | `tools/stage2_scan.py:64`; `tools/research/*.py` |
| Fundamentals | Yahoo `yf.Ticker().info` (trailingPE, ROE) | `tools/value_screen.py:45-49` |
| Tests / CI | pytest (79 tests) + GitHub Actions (py 3.11/3.12) | `.github/workflows/fallacy-auditor-ci.yml`; `fallacy-auditor/.github/workflows/ci.yml` |
| Index lists | Official NSE constituent CSV fetch | `tools/fetch_nifty500.py:11-16` |

---

## 5. HOW THE CORE MECHANISMS WORK (from source)

### 5a. Weinstein entry/exit as coded (`stock_stage2_trend_weekly.pine`)
```
sma30  = ta.sma(close, 30)                    # :66
anchor = ta.highest(close[1], 52)             # :67  prior-week closes only
rising = sma30 > sma30[4]                      # :68
stage2Break = close > anchor and close > sma30 and rising   # :70
doEnter = stage2Break and strategy.position_size == 0        # :73  flat-only
if doEnter: strategy.entry("L", strategy.long)               # :75  LONG only
if strategy.position_size > 0 and close < sma30:             # :84
    strategy.close("L", ...)                                 # :85  exit
```
Matches the description exactly: new 52-week high **and** close above a rising
30-week SMA to enter; close below the SMA to exit; strictly long-only
(`pyramiding = 0` at `:42`); no target. Fills at confirmed close (`:43-44`).

### 5b. The grounding gate (`grounding.py` + `audit.py`)
```
# grounding.py:32-44
for finding in findings:
    if finding in seen: continue          # dedupe
    if finding.span in input_text:        # :36  literal, case-sensitive
        grounded.append(finding)
    else:
        discarded.append(finding)         # dropped, never repaired
# audit.py:56,63
grounded, discarded = ground_findings(parsed.findings, input_text)
return AuditReport(findings=grounded)     # raw LLM findings never returned
```
The public API (`audit_text`) can only return findings whose `span` is a
verbatim substring of the input. There is no code path that returns an
ungrounded finding — the gate is inescapable, and it is covered by dedicated
tests (`tests/test_grounding.py`). This is the strongest, most novel, most
defensible engineering claim in the repo.

---

## 6. QUANTIFIABLE FACTS (committed & verifiable right now)

| Fact | Value | Source |
|---|---|---|
| Fallacy types detected | **5** | `schemas.py` enum (survivorship, lookahead, overfitting, base-rate, unfalsifiable) |
| Hand-labeled eval examples | **44** (38 labels, 10 clean) | `data/labeled_examples.jsonl` (line count) |
| Automated tests | **79** | `pytest --collect-only` |
| Python package modules | 12 | `src/fallacy_auditor/*.py` |
| Pine linter rules | **5** | `pinelint.py:15-28` |
| Pine strategy/indicator files | 23 `.pine` files | `src/*.pine` |
| Weekly-strategy universe (default watchlist) | ~160 names | `tools/stage2_scan.py:37-55` |
| Nifty-500 watchlist (committed) | 500 symbols | `tools/watchlist_nifty500.txt` |
| CI matrix | Python 3.11 & 3.12 | CI yml |
| License | MIT | `fallacy-auditor/LICENSE`, `pyproject.toml:11` |

*(Performance numbers — PF, CAGR, P/R — are deliberately NOT in this table;
they belong in §3 as reproducible-but-not-committed.)*

---

## 7. DOCS/CLAIMS-VS-CODE DISCREPANCIES (do NOT put these on the resume)

> **FIXES APPLIED (post-audit):** (1) & (4) cost model reconciled — the Pine
> champion now uses 0.25%/side commission to match the replication, with
> slippage documented as additional conservatism (`stock_stage2_trend_weekly
> .pine:53`). (2) a committed `eval_report` snapshot and pooled backtest CSVs
> now live in `results/` + `fallacy-auditor/results/`, so the headline numbers
> are inspectable without re-running. (3) `test_eval.py` docstring corrected to
> say the default engine is free local Ollama. (5) dead `simulate()` deleted
> from `replicate_stage2.py`. (6) remains a standing framing note. The original
> findings are kept below as the audit record.


1. **Two different cost models.** Docs/chat say "0.25%/side"; the Pine strategy
   actually uses 0.2%/side + 5 ticks slippage (`stock_stage2_trend_weekly.pine
   :49-50`), while only the Python replications use 0.25% (`replicate_stage2.py
   :28`). Don't claim a single consistent cost assumption.
2. **Eval numbers are not in the repo.** The README/chat cite two-pass
   P 0.88 / R 0.74, but `eval_report.json` is gitignored (`.gitignore:5`) — no
   committed artifact proves it. Only claim it as "reproducible by running the
   harness," and ideally commit one eval report to make it verifiable.
3. **`test_eval.py` docstring is stale.** It says the eval "needs Anthropic
   credentials and spends tokens" (`:3-5`), but the code defaults to the free
   local Ollama engine (`:133`). Cosmetic, but a reader will notice.
4. **Pine header says "~0.2%/side" while calling it the tested config**, yet
   the "tested" numbers in the same header (`:13-18`) came from the 0.25%
   Python path. Same issue as (1), inside one file.
5. **Dead `simulate()` function** in `replicate_stage2.py:~40-66` (superseded
   by `run()`); its `equity += ... + 0  # handled below` line looks buggy. It
   doesn't affect results but shouldn't ship.
6. **"Passed its pre-registered bar" is a process claim, not a code fact.** The
   pre-registration lived in strategy headers/chat, not in a timestamped
   committed artifact predating the test. Defensible as your methodology, not
   as something the repo cryptographically proves.

---

## 8. SECURITY / HYGIENE — **CLEAN**

- No secrets, API keys, or tokens in any tracked file (grep for `ghp_`,
  `sk-ant-`, `ANTHROPIC_API_KEY=`, private-key headers → none).
- The GitHub PAT is **not** in the repo (it lives in `~/.git-credentials`,
  outside version control). **Still rotate it** — it was pasted into a chat.
- No result artifacts leak personal data: `journal.csv`, `breadth_log.csv`,
  `eval_report.json`, `data_cache/` are all gitignored (root `.gitignore` +
  `fallacy-auditor/.gitignore`).
- One nit: `.gitignore` references `files.zip`, `git_*.deb`, `.localgit/` —
  local tooling detritus; fine, just confirm none is committed (they aren't).

---

## 9. RESUME OUTPUT

### 9a. RESUME-SAFE CLAIMS (each defensible from code)
- Built an MIT-licensed Python package that audits LLM-generated trading
  reasoning for 5 specific fallacies using a **local, free** open-weights model
  (Ollama/qwen2.5:7b) — no paid API. `llm.py:134-159`, `schemas.py`.
- Designed a **grounding gate** that structurally prevents hallucinated
  critiques by discarding any model finding whose cited span is not a verbatim
  substring of the input; the public API cannot emit an ungrounded finding.
  `grounding.py:36`, `audit.py:56-63`, tests in `test_grounding.py`.
- Wrote a **44-example hand-labeled evaluation set** and a precision/recall
  harness that scores the auditor and logs regressions. `data/labeled_examples
  .jsonl`, `tests/test_eval.py`.
- Shipped **79 automated tests** and **GitHub Actions CI** (Python 3.11/3.12).
- Built a deterministic **Pine Script linter** (5 mechanical-bias rules) and a
  **profit-factor fragility auditor** (bootstrap 95% CI, luck-concentration and
  time-stability checks) in the standard library. `pinelint.py`, `profit.py`.
- Implemented a **Weinstein Stage-2 weekly trend strategy** in Pine and a
  Python **scanner** over the Nifty 500 with completed-bar (non-repainting)
  enforcement and a liquidity gate. `stock_stage2_trend_weekly.pine`,
  `stage2_scan.py:84,88`.
- Built a **replication harness** that recomputes strategy profit factor and
  breadth with transaction costs and bootstrap CIs across Indian and US
  universes, and used it to **reject 7 candidate strategies against
  pre-registered criteria** (documented). `tools/research/*.py`.

### 9b. DO-NOT-CLAIM (asserted, biased, or drifty)
- ❌ "Achieved a profit factor of 6.29 / 3.53 / etc." — survivorship-biased,
  not committed, drifts with live data. Say "measured in backtests I ran,"
  never as a result the repo proves.
- ❌ "23.8% CAGR strategy" as a performance credential — same reasons; and it's
  a backtest with no forward/live track record.
- ❌ "Auditor achieves 0.88 precision" as a hard number — the eval report isn't
  committed; only claim the *harness exists* unless you commit a report.
- ❌ Any phrasing implying live/real-money results — the system is at the
  paper-trading gate; nothing has traded.
- ❌ "Validated" strategy in a strong sense — it passed *backtest* bars, not a
  forward test.

### 9c. DRAFT BULLETS
- **Engineered** an MIT-licensed Python fallacy-auditing package that flags 5
  reasoning fallacies in LLM-generated trading text via a local Ollama model,
  with a novel *grounding gate* (verbatim-substring verification) that
  structurally eliminates hallucinated findings; 79 tests + CI. **[DEFENSIBLE]**
- **Built** a 44-example labeled eval set and a precision/recall harness with a
  two-pass LLM verifier, driving prompt/model iteration measured on gold
  labels. **[DEFENSIBLE]**
- **Developed** a Weinstein Stage-2 weekly trend system (Pine + Python scanner)
  with non-repainting completed-bar logic, and a replication harness that
  stress-tests profit factor with bootstrap CIs and costs across Indian & US
  universes — used to reject 7 strategies against pre-registered bars.
  **[DEFENSIBLE]** *(keep it about the harness + discipline, not the PF value)*
- **Measured** two-pass auditor precision/recall on a local 7B model
  (qwen2.5:7b). **[VERIFY — commit an `eval_report.json` first, then cite the
  number]**

---

## 10. HARDEST-TO-DEFEND — the 5 questions to prepare for

1. **"Your backtest universe is today's Nifty 500 — how much of that PF is
   survivorship bias?"** The killer question. Honest answer: material,
   especially for a trend system; that's why you lead with breadth and a
   stripped ~15-16% floor, and why the real test is forward paper-trading. Be
   ready to quantify direction even if you can't fully correct it.
2. **"Show me the number. Where in the repo is PF 6.29 / the 0.88 eval?"** They
   aren't committed. Prepare to either (a) run the script live in the
   interview, or (b) commit the result artifacts beforehand. Right now this is
   your weakest spot — fix it by committing one eval report and one pooled
   result CSV with a README pointer.
3. **"Your Pine tester uses 0.2% costs but your Python uses 0.25% — which is
   the result, and why two?"** Reconcile the two cost models or state which
   engine produced which number.
4. **"A weekly-close signal — how do you avoid acting on a forming bar live,
   and did the backtest ever do so?"** Backtest is clean (confirmed closes);
   the discipline is code-enforced in the scanner (`:84`) but human-enforced in
   execution. Know the difference cold.
5. **"How do you know the 52/30/4 parameters weren't curve-fit?"** They're
   Weinstein's published canonical values (1988), chosen a priori, with no
   optimizer in the repo — but you cannot *prove* absence of tuning from code.
   Frame as "fixed literature parameters," and mention the pre-registration
   discipline and the 7 documented rejections as evidence of a non-overfitting
   process.

*Priority fixes before interviewing: commit one `eval_report.json` and one
pooled result CSV (turns §3 numbers from asserted → verifiable); reconcile the
0.2%/0.25% cost split; delete the dead `simulate()`; rotate the PAT.*
