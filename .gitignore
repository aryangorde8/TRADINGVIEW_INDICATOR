files.zip
.localgit/
git_*.deb

# python/tooling
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
tools/journal.csv
tools/breadth_log.csv
# data_cache/ is NOT ignored: the parquet price snapshot is pinned and
# committed so published backtest figures are reproducible (MANIFEST.md).
