name: fallacy-auditor-ci

on:
  push:
    paths: ["fallacy-auditor/**", ".github/workflows/fallacy-auditor-ci.yml"]
  pull_request:
    paths: ["fallacy-auditor/**"]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fallacy-auditor
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[dev]"
      - run: pytest -q
      - run: python scripts/validate_dataset.py data/labeled_examples.jsonl
